# 2026-09-17 — CNN-only verification

- Simplified the application to one injected CNN classifier and removed the
  model selector from the webcam page and JavaScript.
- Removed the unused legacy detector copy and unused alternative model assets.
  Updated the retained-source manifest and CNN runtime dependency lock.
- Removed obsolete model-selection documentation; retained the adapter boundary
  for testing and CNN upgrades. Original capstone application is unchanged.
- Validation: 15 automated tests pass, including capture restart, classifier
  errors, and rejection of model-selection requests. JavaScript syntax passes.
- Existing authentication and evidence-window behavior remain in place.
